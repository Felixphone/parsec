package engine.physics.objects.boundings;

import engine.maths.Vector3f;

public class BoxBound extends Bound{
    public BoxBound(Vector3f position, Vector3f rotation, Vector3f size) {
        super(position, rotation, size);
    }
}
