package engine.physics.objects.boundings;

import engine.maths.Vector3f;

public class AlignedBoxBound extends Bound {

    public AlignedBoxBound(Vector3f position, Vector3f rotation, Vector3f size) {
        super(position, rotation, size);
    }
}
